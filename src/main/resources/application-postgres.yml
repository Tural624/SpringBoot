spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/spring
    username: postgres
    password: arxideya464
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect